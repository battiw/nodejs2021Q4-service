 meta: true, // optional: control whether you want to log the meta data about the request (default to true)//необязательно: укажите, хотите ли вы регистрировать метаданные о запросе (по умолчанию true)
 


msg: "AUTHORIZEDROUTER: HTTP {{req.method}} {{req.url}} {{res.statusCode}} {{res.responseTime}}ms", // optional: customize the default logging message. E.g. "{{res.statusCode}} {{req.method}} {{res.responseTime}}ms {{req.url}}"
  //expressFormat: true, // Use the default Express/morgan request formatting. Enabling this will override any msg if true. Will only output colors with colorize set to true//expressFormat: true, // Использовать форматирование запроса Express / morgan по умолчанию. Включение этого параметра переопределит любое сообщение, если оно истинно. Будут выводить цвета только с установкой colorize на true


colorize: true, // Color the text and status code, using the Express/morgan color palette (text: gray, status: default green, 3XX cyan, 4XX yellow, 5XX red).//Раскрасьте текст и код состояния, используя цветовую палитру Express / morgan (текст: серый, состояние: зеленый по умолчанию, 3XX голубой, 4XX желтый, 5XX красный).


ignoreRoute: function (req, res) { return false; } // optional: allows to skip some log messages based on request and/or response //необязательно: позволяет пропустить некоторые сообщения журнала на основе запроса и / или ответа


expressFormat: true, // Use the default Express/morgan request formatting. Enabling this will override any msg if true. Will only output colors with colorize set to true // Используйте форматирование запросов Express / morgan по умолчанию. Включение этого параметра переопределит любое сообщение, если оно истинно. Будут выводить цвета только с установкой colorize на true

metaField: null, //this causes the metadata to be stored at the root of the log entry//это приводит к тому, что метаданные хранятся в корне записи журнала.


responseField: null, // this prevents the response from being included in the metadata (including body and status code)//это предотвращает включение ответа в метаданные (включая текст и код состояния)



requestWhitelist: ['headers', 'query'],  //these are not included in the standard StackDriver httpRequest//они не включены в стандартный httpRequest StackDriver



responseWhitelist: ['body'], // this populates the `res.body` so we can get the response size (not required) //это заполняет res.body, чтобы мы могли получить размер ответа (не обязательно)



